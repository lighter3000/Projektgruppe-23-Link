tutorial_text = "Zeichene ein rotes und gefülltes Rechteck! Nutze für die Kantenlänge und den Winkel jeweils Variablen!<br/><br/>Durch Varibalen ist Code übersichtlicher und kann leichter bearbeitet werden. Dadurch muss man bei Änderungen oft nur an einer Stelle was ändern.<br/><br/>Um eine Form zu füllen muss man vor dem Zeichenen der Form t.begin_fill() und danach t.end_fill() aufrufen.<br/><br/>"
tutorial_code = "t.begin_fill()\t# Form füllen starten bevor Form gezeichnet wurde\n# Form zeichnen\nt.end_fill()\t# Form füllen beenden nachdem Form gezeichnet wurde\n\nnumber = 7\t# definiert Variable namens \"number\" mit Wert 7. Um andere Zahlen auszugeben, muss man nur die Variable ändern\n\n# gibt den Wert der Variable \"number\" auf der Konsole dreimal aus.\nprint(number)\nprint(number)\nprint(number)\n\nKonsole: 7 7 7"
init_code = "### imports + setup\nfrom browser import document\nimport turtle\nturtle.set_defaults(turtle_canvas_wrapper = document['canvas'])\nt = turtle.Turtle()\nt.shape(\"turtle\")\n\n### execution\n\n\nturtle.done()\t\t# turtle Ausführung beenden"
written_code = "### imports + setup\nfrom browser import document\nimport turtle\nturtle.set_defaults(turtle_canvas_wrapper = document['canvas'])\nt = turtle.Turtle()\nt.shape(\"turtle\")\n\n### execution\n\n\nturtle.done()\t\t# turtle Ausführung beenden"
solution_code = "### imports + setup\nfrom browser import document\nimport turtle\nturtle.set_defaults(turtle_canvas_wrapper = document['canvas'])\nt = turtle.Turtle()\nt.shape(\"turtle\")\n\n### execution\nlength = 100\t\t# Kantenlänge\nangle = 90\t\t# Winkel\n\nt.color('red')\t\t# ändert Farbe des Stiftes und der Schildkröte auf rot\n\nt.begin_fill()\n\nt.forward(length)\t# 100 Schritte nmachvorne\nt.right(angle)\t\t# Schildkröte um 90 Grad nach rechts drehen\n\n# das gleiche wie oben noch 3 mal\nt.forward(length)\nt.right(angle)\n\nt.forward(length)\nt.right(angle)\n\nt.forward(length)\nt.right(angle)\n\nt.end_fill()\n\nturtle.done()\t\t# turtle Ausführung beenden"